{
  "name": "@onchain-gallery/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/hashes": "^1.3.3",
    "bs58check": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2",
    "vitest": "^1.6.0"
  }
}
